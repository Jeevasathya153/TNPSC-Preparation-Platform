# Spring Application Configuration

# Server configurations
server.port=${PORT:8080}
server.servlet.context-path=/api

# MongoDB Database Connection
spring.data.mongodb.uri=mongodb+srv://tngov_exam_prep:tngov_exam_prep@new-cluster.ww4x2jm.mongodb.net/tngov_exam_prep?appName=New-Cluster

# Authentication (JWT configs for production environment)
jwt.secret=${JWT_SECRET:production_refinement_tngov_exam_prep_secret_key_2026}

# Mail Server credentials (placeholders for email notifications)
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}

# AI Service API integration keys
gemini.api.key=${GEMINI_API_KEY:}
openai.api.key=${OPENAI_API_KEY:}

# Cloudinary assets keys
cloudinary.api.key=${CLOUDINARY_API_KEY:}
cloudinary.api.secret=${CLOUDINARY_API_SECRET:}
