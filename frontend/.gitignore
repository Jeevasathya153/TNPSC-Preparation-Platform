# Node dependencies
node_modules/

# Environment configurations
# .env contains local dev values (safe to commit — uses /api proxy, no secrets)
# .env.production and .env.local must NEVER be committed (may contain real URLs/secrets)
.env.local
.env.production
.env

# Compiler and Bundler outputs
dist/
build/
target/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# IDE settings and OS files
.vscode/
.expo/
.DS_Store
Thumbs.db
